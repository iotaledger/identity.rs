{
  "name": "iota-identity-wasm-test",
  "version": "0.0.10",
  "description": "WASM binding for the IOTA identity library",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/iotaledger/identity.rs.git"
  },
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "build:nodejs": "wasm-pack build --target nodejs --out-dir node && rm ./node/.gitignore",
    "build:web": "wasm-pack build --target web --out-dir web && node ./build/web && rm ./web/.gitignore",
    "build": "npm run build:web && npm run build:nodejs",
    "pretest": "npm run build:nodejs",
    "prepublishOnly": "npm run build",
    "test": "mocha",
    "serve": "webpack-dev-server",
    "example:node": "node examples/node.js"
  },
  "contributors": [
    "huhn511 <huhn@einfachiota.de>"
  ],
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/iotaledger/identity.rs/issues"
  },
  "homepage": "https://www.iota.org",
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "web",
    "node",
    "web.js"
  ],
  "devDependencies": {
    "@wasm-tool/wasm-pack-plugin": "^1.1.0",
    "copy-webpack-plugin": "^6.3.0",
    "mocha": "^7.1.2",
    "rimraf": "^3.0.0",
    "webpack": "^4.42.0",
    "webpack-cli": "^3.3.3",
    "webpack-dev-server": "^3.7.1"
  },
  "dependencies": {
    "node-fetch": "^2.6.0"
  }
}