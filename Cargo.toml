[workspace]
resolver = "2"
members = [
  "identity",
  # "identity-comm",
  "identity-account",
  "identity-account-storage",
  "identity-actor",
  "identity-core",
  "identity-credential",
  "identity-did",
  "identity-diff",
  "identity-iota",
  "identity-iota-core",
  "examples",
]

exclude = [
  "bindings/stronghold-nodejs",
  "bindings/wasm",
  "libjose",
]

[profile.dev]
split-debuginfo = "unpacked"
